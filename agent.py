# Atalho para evitar confusão no Railway/console.
# Pode rodar tanto `python agent.py` quanto `python followup_agent.py`.
from followup_agent import rodar_followup

if __name__ == "__main__":
    rodar_followup()
